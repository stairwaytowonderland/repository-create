name: Release

on:
  push:
    branches: [main]
    # paths:
    #   - 'src/**'
    #   - 'package*.json'
    #   - 'action.yaml'
  workflow_dispatch:

jobs:
  release:
    name: Semantic release
    runs-on: ubuntu-latest

    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT_CREATE_REPO }}

      - name: Semantic release
        id: release
        uses: ./.github/actions/node-semantic-release
        with:
          github-token: ${{ secrets.GH_PAT_CREATE_REPO }}

      - name: Dispatch Publish workflow
        if: steps.release.outputs.new-release-published == 'true'
        uses: ./.github/actions/workflow-dispatch
        with:
          workflow: publish.yaml
          tag: ${{ steps.release.outputs.new-release-git-tag }}
          fields: |
            notes-b64=${{ steps.release.outputs.new-release-notes-base64 }}
